# Development Environment Variables
# LEER = relative URLs → Vite Proxy leitet /api/* an localhost:8080 weiter
# So bleibt der Cookie auf dem gleichen Origin (localhost:5173)
VITE_API_URL=
VITE_APP_NAME=BrainyTrader
VITE_APP_VERSION=1.0.0
